{
  "name": "interactive-content-designer",
  "version": "1.0.0",
  "repository": "https://github.com/jigjoy-io/interactive-content-designer.git",
  "author": "Mijura <miodragvilotijevic@gmail.com>",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "serve": "wsrun --parallel serve",
    "start": "concurrently \"wsrun --parallel start\" \"wsrun --parallel serve\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^5.1.0",
    "wsrun": "^5.2.0"
  }
}
